Font binaries are intentionally not included in this clean handoff package.

The UI uses system/browser fallbacks declared in `src/styles.css`. If the project owner has licensed brand fonts, add them in a private deployment pipeline and update the CSS intentionally.
